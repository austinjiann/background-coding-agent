{
  "name": "phoebe-backend",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "env -i HOME=$HOME PATH=$PATH bun --env-file ../.env --watch index.ts",
    "start": "env -i HOME=$HOME PATH=$PATH bun --env-file ../.env index.ts"
  }
}
